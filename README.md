# Email-Spam-Detector

## Test the Saved Model

This repo includes a small balanced test dataset in `test_data.csv` and a
smoke test for the saved vectorizer/model artifacts.

Run:

```bash
python3 -m unittest test_model.py -v
```
